# ============================================================
# requirements.txt — toàn bộ project ai-threat-intel
# ============================================================
#
# Cài đặt:
#   pip install -r requirements.txt
#
# Mỗi layer chỉ phụ thuộc vào các package trong nhóm tương ứng,
# nhưng để dev/CI đơn giản ta gộp chung vào một file.

# ────────────────────────────────────────────────────────────
# Layer 1 — Collectors (Hùng)
# ────────────────────────────────────────────────────────────
httpx==0.27.0
feedparser==6.0.11
trafilatura==1.12.0
sqlalchemy==2.0.30
beautifulsoup4==4.12.3
python-dotenv==1.0.1

# ────────────────────────────────────────────────────────────
# Layer 2 — Normalization & Cleaning (Vinh)
# ────────────────────────────────────────────────────────────
stix2==3.0.1                      # STIX 2.1 object builder/validator
datasketch==1.6.5                 # MinHash + LSH cho near-duplicate dedup
lingua-language-detector==2.0.2   # Language detection (đa ngôn ngữ)
markdown-it-py==3.0.0             # Parse markdown để strip về plain text

# ────────────────────────────────────────────────────────────
# Dev / test
# ────────────────────────────────────────────────────────────
pytest==8.2.2                     # unit tests

# ────────────────────────────────────────────────────────────
# Layer 3 — Entity Extraction (Vinh)
# ────────────────────────────────────────────────────────────
iocextract==1.16.1                # Tier 1: regex IOC + defang/refang
# spacy==3.7.5                    # Tier 2 (sẽ dùng sau): NER malware/actor/tool

# ────────────────────────────────────────────────────────────
# Layer 6 — Storage & Interface (Vinh) — sẽ dùng sau
# ────────────────────────────────────────────────────────────
# psycopg[binary]==3.2.1          # PostgreSQL driver
# sentence-transformers==3.0.1    # Embeddings cho vector DB
# chromadb==0.5.3                 # Vector DB (lựa chọn đơn giản)
# streamlit==1.36.0               # Dashboard
